# Railway (and other buildpacks) look for a flat requirements.txt at the
# project root by convention. The real, maintained dependency list lives in
# requirements/ — base.txt for the app, production.txt adding the process
# manager and ASGI worker — so this just points there rather than
# duplicating it and risking drift between two copies of the same list.
-r requirements/production.txt
