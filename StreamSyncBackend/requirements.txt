# Render (and other buildpacks) look for a flat requirements.txt at the
# project root by convention. The real, maintained dependency list lives in
# requirements/ — base.txt for the app, production.txt adding the process
# manager and ASGI worker — so this just points there rather than
# duplicating it and risking drift between two copies of the same list.
#
# Do not hand-edit this into a flat pinned list again — that's what caused
# the uvicorn/psycopg mismatch this file previously shipped with. Add or
# change a dependency in requirements/base.txt or requirements/production.txt
# instead; this file should only ever contain the line below.
-r requirements/production.txt
